#include <bits/stdc++.h>
using namespace std;
char c[2000000];
int w;
int main()
{
	ios::sync_with_stdio(false);
	cin >> w;
	char input=1;
	int now = 1;
	while (input!=EOF)
	{
		input=getchar();
		if()
	};
	return 0;
}